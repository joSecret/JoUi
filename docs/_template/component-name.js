// @ts-nocheck — placeholder tokens are invalid JS until scaffold-time substitution
((Drupal) => {
  Drupal.behaviors.{{component_name}} = {
    attach(context) {
      // Example: context.querySelectorAll('.{{component-name}}').forEach((el) => { ... });
    },
  };
})(Drupal);
